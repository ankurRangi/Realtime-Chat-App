// Any types that has to do with owr database, d.ts will let you use it anywhere in the project without even importing

interface User {
  name: string;
  email: string;
  image: string;
  id: string;
}
